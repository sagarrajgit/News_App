class Article{
  const Article({
    required this.name,
    required this.title,
    required this.url,
    required this.urlToImage,
    required this. publishedAt,
  });

  final String name, title, url, urlToImage, publishedAt;
}